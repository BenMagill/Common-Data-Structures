class LinkedList {
    constructor(){
        this.list = [] // Dynamic size as the idea is it can be expanded infinitly
        this.startPointer = 0 // Location of first item
    }

    insert(){}

    addToEnd(){}

    addToStart(){}

    remove(){}

    lastItemIndex(){
        var currIndex = this.startPointer
        var nextIndex = undefined
        var found = false
        while (found !== true){
            
        }
        return currIndex

        /**
         * Access first location
         * Does next index = 0?
         * No go to it and repeat
         * Yes return index at
         */
    }
}

exports.default = LinkedList